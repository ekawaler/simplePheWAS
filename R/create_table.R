# Just calls createPhewasTable. Basically just exists
# so all the function names can be formatted with underscores.

create_phewas_table <- function(id.icd9.count, ...){
  createPhewasTable(id.icd9.count, ...)
}
